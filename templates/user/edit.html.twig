{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1><a class="btn btn-primary" href="{{ path('user_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> Edit User</h1></div>
        <div class="card-body">
            {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}


            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}
