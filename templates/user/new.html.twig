{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1><a href="{{ path('user_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a>  Create new User</h1>
        </div>
        <div class="card-body">
            {{ include('user/_form.html.twig') }}

        </div>
    </div>


{% endblock %}
