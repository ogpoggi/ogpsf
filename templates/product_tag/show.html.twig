{% extends 'base.html.twig' %}

{% block title %}ProductTag{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1><a href="{{ path('product_tag_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> ProductTag</h1></div>
        <div class="card-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ product_tag.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ product_tag.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ product_tag.description }}</td>
                </tr>
                </tbody>
            </table>
            <a class="btn btn-warning btn-sm" href="{{ path('product_tag_edit', {'id': product_tag.id}) }}">edit</a>

            {{ include('product_tag/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}
