{% extends 'base.html.twig' %}

{% block title %}Edit Review{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('review') }}
            <script src="/build/runtime.js"></script>
            <script src="/build/review.js"></script>
        {% endblock %}
{% block body %}
    <div class="card">
        <div class="card-header"><h1><a class="btn btn-primary" href="{{ path('review_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> Edit Review</h1></div>

            {{ include('review/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        {{ include('review/_delete_form.html.twig') }}

    </div>
{% endblock %}
